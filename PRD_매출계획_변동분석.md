# PRD: 매출 실행계획 주간 변동 분석 앱

| 항목 | 내용 |
|---|---|
| 문서명 | Excel 매출계획 변동(주차 대비) 분석 웹앱 |
| 버전 | v1.0 |
| 작성일 | 2026-09-01 |
| 분석 기준 데이터 | `후판) □□영업본부_'26년 실행계획_신규양식_실습용 더미데이터_8-1주.xlsx` / `8-2주.xlsx` |
| 상태 | 초안 (개발 착수 전) |

---

## 1. 개요

매출 실행계획은 매주 갱신된다. 같은 양식의 Excel 파일이 주차별로 쌓이지만, **"지난주 대비 이번주에 뭐가 얼마나 바뀌었는지"** 를 확인하려면 지금은 두 파일을 나란히 열고 눈으로 대조하거나 VLOOKUP을 새로 짜야 한다. 277행 × 55열 규모에서는 실수하기 쉽고, 바뀐 이유(물량이 빠진 건지 단가가 내린 건지)까지는 알 수 없다.

이 앱은 **변동 전/후 두 파일을 올리면 자동으로 행을 매칭해 증감을 계산하고, 변동의 원인을 물량효과와 단가효과로 분해**한다.

### 실행 방식 — Streamlit 로컬 웹앱
**Streamlit**으로 구현한다. 최초 1회 Python 환경과 패키지만 설치하면, 이후에는 배치 파일 더블클릭 한 번으로 로컬 서버가 뜨고 브라우저가 자동으로 열린다. 빌드 과정은 없고, 코드를 수정하면 브라우저에서 새로고침만 하면 된다.

서버는 **localhost에서만 동작**하므로 업로드한 Excel은 PC 밖으로 나가지 않는다. 사내 매출·손익 데이터를 다루므로 이 점이 중요하다.

> **선행 조건**: 현재 이 PC에는 실제 Python이 설치되어 있지 않다(Microsoft Store 스텁만 존재). 개발 착수 전 Python 3.11+ 설치가 필요하며, 설치 절차는 §7.6에 정리했다.

---

## 2. 목표 / 비목표

### 목표
| # | 목표 | 성공 기준 |
|---|---|---|
| G1 | 두 주차 파일의 변동을 자동 산출 | 현행 규모(277행) 기준 업로드 후 3초 이내 결과 표시 |
| G2 | 변동 원인 규명 | 금액 증감을 물량효과 / 단가효과로 잔차 없이 분해 |
| G3 | 기간 축 전환 | 월·분기·반기·연간 집계를 클릭 한 번으로 전환 |
| G4 | 드릴다운 | 특정 기간 선택 시 기여도 상위 항목까지 추적 |
| G5 | 간편 실행 | 최초 1회 설치 후, 실행 배치 더블클릭 → 브라우저 자동 오픈 |

### 비목표 (v1 제외)
- 3개 주차 이상 동시 비교 (시계열 추이) — v2
- 원본 Excel 수정·저장 기능
- 서버 저장, 이력 관리, 사용자 계정
- 후판 외 다른 사업부 양식 자동 인식 (양식이 동일하면 동작하나, 검증 범위는 후판 양식)

---

## 3. 입력 데이터 명세 (실제 파일 분석 결과)

### 3.1 시트 구조

- 시트명: **`후판`** (단일 시트)
- 데이터 범위: **`A1:BC282`** → 55개 컬럼, **데이터 277행**
- 헤더가 5행 구조로 되어 있어 일반적인 "1행=헤더" 가정이 통하지 않는다:

| 행 | 내용 | 처리 |
|---|---|---|
| 1행 | 숫자 파라미터 (A=1000, Y=400 …) | 무시 |
| 2행 | 안내 문구 (`※ 초록색: 표준원가 맵핑을 위한 필수Key필드`) | 무시 |
| 3행 | **단위** (톤 / 백만원 / 천원/톤 / $/톤 / 백만$ / %) | 화면 단위 표기에 사용 |
| 4행 | **기존** 양식 컬럼명 | 무시 (참고용) |
| 5행 | **변경(신규)** 양식 컬럼명 | **실제 헤더로 사용** |
| 6행~282행 | 데이터 | 파싱 대상 |

> 파서는 헤더를 행 번호로 하드코딩하지 말고, **A열이 `변경`인 행을 찾아 헤더 행으로 인식**한다. 향후 양식에 행이 추가돼도 견딘다.

### 3.2 컬럼 분류

**① 차원(Dimension) — 18개, 행 매칭 키**

| 열 | 컬럼명 | 열 | 컬럼명 |
|---|---|---|---|
| B | 사업부 | K | 내수수출 |
| C | 영업실 | L | 회의자료_대분류 |
| D | 공장 | M | 팀파트 |
| E | 제품군 대분류 | N | 고객그룹 |
| F | 품종 | O | 글로벌_고객그룹 |
| G | 품명 | P | 정품구분 |
| H | 규격_분류 | Q | 비고 |
| I | 제품_특성 | R | 고객명 |
| J | 제품_용도 | S | 권역명 |

**② 시점(Time)**

| 열 | 컬럼명 | 비고 |
|---|---|---|
| T | 기준년월 | Excel 날짜 serial (예: 46023 = 2026-01-01) |
| U | 분기 | 1~4 (사전 계산됨) |
| V | 반기 | 1~2 (사전 계산됨) |

**③ 측정값(Measure) — 핵심**

| 열 | 컬럼명 | 단위 |
|---|---|---|
| X | 수량 | 톤 |
| Y | 금액 | 백만원 |
| Z | 단가 | 천원/톤 |
| AA / AB / AC | Base / Extra / 운임 | 천원/톤 (단가 구성요소) |
| W | 환율 | — |
| AD~AG | USD / FOB.E / 선임 / 관세 | $/톤 |
| AH~AK | USD(금액) / FOB.E(금액) / 선임(금액) / 관세(금액) | 백만$ |
| AL / AM / AN / AO | 영업이익 / 손익 / % / 총원가 | 백만원, % |
| AQ~AZ | 그룹원가 연동 항목 (수량·금액·총원가·영업이익·경상이익 등) | 백만원 |
| BA / BB / BC | Basex수량 / Extrax수량 / 운임x수량 | 천원 (검산용) |

### 3.3 검증된 데이터 관계식

실제 값으로 확인한 항등식이며, 앱의 계산 로직과 검산의 근거가 된다.

```
단가(Z) = Base(AA) + Extra(AB) + 운임(AC)
   예) 1,435.77 + 294.94 + 39.12 = 1,769.83 ✓

금액(Y, 백만원) = 수량(X, 톤) × 단가(Z, 천원/톤) ÷ 1,000
             = (BA + BB + BC) ÷ 1,000
   예) (13,132,988 + 2,697,816 + 357,830) ÷ 1,000 = 16,188.635 ✓
```

이 관계 덕분에 **금액 변동을 물량·단가로, 다시 단가를 Base/Extra/운임으로 분해**할 수 있다.

### 3.4 행 매칭 키의 유일성 (검증 완료)

`B~S 차원 18개 + T 기준년월` 조합으로 키를 만들면:

```
전체 277행 → 유니크 키 277개 (중복 0)
```

**이 복합키가 변동 전/후 행 매칭의 기준이 된다.** 특히 6·7·8행은 `비고`(전기협상가 / 당기협상가 / 기타)만 다르므로, **비고 컬럼을 키에서 빼면 안 된다.**

### 3.5 샘플 데이터 실측 (8-1주 → 8-2주)

| 기준년월 | 8-1주 금액 | 8-2주 금액 | 증감 |
|---|---:|---:|---:|
| 2026-01 ~ 2026-06 | (동일) | (동일) | 0 |
| **2026-07** | **1,430,709.9** | **1,419,218.7** | **−11,491.2 (−0.80%)** |

- 전체 277행 중 **26행 변동**, 전부 2026-07월에 집중
- 수량 1,075,073톤 → 1,067,673톤 (**−7,400톤**)
- 예시 행: 수량은 18,368톤으로 동일한데 Base 단가가 1,111.12 → 1,101.12로 하락 → 금액 감소
  → **물량 변동과 단가 변동이 함께 발생**하므로 효과 분해가 실제로 필요하다.
- 샘플 데이터의 계획 기간은 2026-01 ~ **2026-07(7개월)** 이지만, 앱은 **데이터에 존재하는 월을 동적으로 인식**해 1~12월 어떤 구성이든 처리한다.

---

## 4. 기능 요구사항

### 4.1 파일 업로드 (FR-100)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-101 | **변동 전(전주)** / **변동 후(금주)** 2개 드롭존, 드래그앤드롭 + 파일 선택 | Must |
| FR-102 | `.xlsx` 파싱, 헤더 행 자동 탐지(A열 `변경`) | Must |
| FR-103 | 두 파일의 컬럼 구성 일치 여부 검증, 불일치 시 차이 컬럼 명시 후 중단 | Must |
| FR-104 | 파일명에서 주차 정보 추출해 라벨로 표시 (예: `8-1주` → `8-2주`) | Should |
| FR-105 | 전/후 파일을 반대로 올렸을 때 **스왑 버튼**으로 즉시 교체 | Should |
| FR-106 | 파싱 진행 상태 및 인식 결과 요약(시트명, 행수, 기간 범위, 컬럼수) 표시 | Must |
| FR-107 | 업로드 파일은 디스크에 저장하지 않고 메모리에서만 처리, localhost 바인딩 | Must |
| FR-108 | 파싱 결과를 캐시(`@st.cache_data`)하여 위젯 조작 시 재파싱 방지 | Must |

### 4.2 행 매칭 및 증감 산출 (FR-200)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-201 | 복합키(차원 18 + 기준년월)로 전/후 행 매칭 | Must |
| FR-202 | 매칭 결과를 **유지 / 신규추가 / 삭제 / 값변동** 4개 상태로 분류 | Must |
| FR-203 | 신규 행은 변동 전 = 0, 삭제 행은 변동 후 = 0으로 처리해 합계 정합성 유지 | Must |
| FR-204 | 수량·금액·단가 및 손익 항목 각각의 절대증감·증감률 계산 | Must |
| FR-205 | 부동소수점 오차 흡수 — 상대오차 1e-9 이하는 "변동 없음"으로 간주 | Must |
| FR-206 | 분모 0(신규 행)일 때 증감률은 `신규`로 표기, `∞`/`NaN` 노출 금지 | Must |
| FR-207 | 키 중복 발생 시 경고 배너 + 중복 키 목록 표시 (합산하지 않고 사용자에게 알림) | Should |

### 4.3 기간 축 선택 (FR-300)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-301 | **월 / 분기 / 반기 / 연간** 4개 집계 단위 토글 | Must |
| FR-302 | 분기·반기는 원본 U·V 컬럼 값을 우선 사용, 없으면 기준년월에서 파생 | Must |
| FR-303 | 축 전환 시 표의 열 구성이 즉시 재구성 (재업로드·재파싱 없이 캐시된 diff에서 재집계) | Must |
| FR-304 | 데이터에 존재하는 기간만 열로 생성 (빈 기간 열 생성 금지) | Must |
| FR-305 | 측정값 전환: 수량(톤) / 금액(백만원) / 단가(천원/톤) | Must |
| FR-306 | 단가는 단순 평균이 아닌 **가중평균**(Σ금액 ÷ Σ수량 × 1,000)으로 집계 | Must |

> **FR-306 주의**: 단가를 행 평균으로 내면 물량 구성이 다른 항목이 뒤섞여 틀린 값이 나온다. 반드시 금액/수량 기반 가중평균으로 계산한다.

### 4.4 메인 표 (FR-400)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-401 | **변동 후** 데이터를 기준으로 표 구성 | Must |
| FR-402 | 행 축(분석 기준) 선택: 팀파트 / 회의자료_대분류 / 내수수출 / 고객그룹 / 품명 / 권역명 | Must |
| FR-403 | 열 축: 선택한 기간 단위 + 합계 열 | Must |
| FR-404 | 각 셀에 **변동 후 값 + 증감**을 함께 표시 (증가 ▲빨강 / 감소 ▼파랑 / 동일 −회색) | Must |
| FR-405 | 증감 표시 모드 전환: 절대값 / 증감률 / 둘 다 | Should |
| FR-406 | 행 축 2단계 계층 확장(예: 팀파트 → 고객그룹) | Should |
| FR-407 | 변동 있는 행만 보기 필터 | Should |
| FR-408 | 총계 행 고정, 헤더/1열 스크롤 고정 | Must |
| FR-409 | 결과 표 Excel/CSV 다운로드 (`st.download_button`) | Should |

### 4.5 상세 분석 (FR-500)

분석 대상 기간을 선택하면 하단에 상세 패널이 열린다. 선택 방식은 **① 표의 행을 직접 클릭**(`st.dataframe(selection_mode="single-row", on_select="rerun")`)과 **② 사이드바 셀렉트박스** 두 가지를 모두 지원한다. ①이 직관적이지만 Streamlit 버전에 따라 동작 차이가 있어, ②를 항상 동작하는 대체 경로로 둔다.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-501 | 요약 카드: 변동 전 / 변동 후 / 증감액 / 증감률 / 변동 행 수 | Must |
| FR-502 | **효과 분해**: 물량효과 · 단가효과 금액 및 비중 | Must |
| FR-503 | 단가효과 세부 분해: Base / Extra / 운임 기여분 | Must |
| FR-504 | **워터폴 차트**: 변동 전 → 물량효과 → Base → Extra → 운임 → 변동 후 | Should |
| FR-505 | 증감 기여도 **Top N**(기본 10) 항목 순위표 (증가/감소 각각) | Must |
| FR-506 | 신규 추가 행 / 삭제 행 목록 별도 표시 | Must |
| FR-507 | 항목 클릭 시 원본 행 단위 전/후 값 비교 (변동 컬럼 하이라이트) | Should |
| FR-508 | 연간 선택 시에는 월별 증감 추이를 함께 표시 | Should |

---

## 5. 변동 분석 로직 (핵심 설계)

### 5.1 증감 정의

```
증감액   Δ = (변동 후) − (변동 전)
증감률   Δ% = Δ ÷ |변동 전| × 100     (변동 전 = 0 이면 "신규")
```

### 5.2 물량효과 / 단가효과 분해

금액은 `수량 × 단가`이므로 증감은 두 요인으로 나뉜다. 아래 방식은 **잔차가 0**이라 합계가 정확히 맞는다.

```
Q0, P0 = 변동 전 수량, 단가        Q1, P1 = 변동 후 수량, 단가

물량효과 = (Q1 − Q0) × P0 ÷ 1,000
단가효과 = Q1 × (P1 − P0) ÷ 1,000
─────────────────────────────────
합계     = Q1·P1 − Q0·P0 = 금액 증감   ✓ (잔차 없음)
```

- **물량효과**: 단가가 그대로라고 가정할 때, 물량이 늘거나 줄어서 생긴 금액 변화
- **단가효과**: 변동 후 물량 기준으로, 단가가 오르내려서 생긴 금액 변화

### 5.3 단가효과 세부 분해

`단가 = Base + Extra + 운임` 이므로 단가효과도 그대로 쪼개진다.

```
Base 기여  = Q1 × (Base1 − Base0)  ÷ 1,000
Extra 기여 = Q1 × (Extra1 − Extra0) ÷ 1,000
운임 기여  = Q1 × (운임1 − 운임0)   ÷ 1,000
────────────────────────────────────────
합계 = 단가효과   ✓
```

> 실제 샘플에서 2026-07 변동은 Base 단가 하락(1,111.12 → 1,101.12)이 상당 부분을 차지한다. 이 분해가 있어야 "왜 줄었나"에 답할 수 있다.

### 5.4 신규/삭제 행 처리

| 상태 | 변동 전 | 변동 후 | 효과 분류 |
|---|---|---|---|
| 신규 추가 | 없음 | 값 있음 | 전액 **물량효과**(신규) — 별도 항목으로 표기 |
| 삭제 | 값 있음 | 없음 | 전액 **물량효과**(삭제) — 별도 항목으로 표기 |
| 값 변동 | 값 있음 | 값 있음 | 5.2 / 5.3 공식 적용 |

신규·삭제는 단가 비교 대상이 없으므로 단가효과로 배분하지 않는다. 이를 섞으면 단가효과가 왜곡된다.

### 5.5 집계 순서 규칙

```
1) 행 단위로 전/후 매칭 → 행별 효과 계산
2) 선택한 기간·행 축으로 효과를 SUM
3) 표시용 단가는 마지막에 Σ금액 ÷ Σ수량 으로 재계산
```

**효과를 먼저 계산하고 나중에 합산**한다. 집계된 평균단가로 효과를 계산하면 구성비 변화(mix)가 단가효과로 잘못 섞여 들어간다.

---

## 6. 화면 설계

Streamlit 표준 레이아웃(`layout="wide"`)을 쓴다. **조작 위젯은 전부 사이드바**, 결과는 메인 영역에 둔다. 위젯이 본문에 섞이면 스크롤할 때마다 컨트롤을 찾아 올라가야 해서 분석 흐름이 끊긴다.

```
┌─ Sidebar ──────────┐┌─ Main ────────────────────────────────────────┐
│                    ││  매출 실행계획 변동 분석                        │
│ 📂 파일            ││  8-1주 → 8-2주 · 277행 · 2026.01~07            │
│ ┌────────────────┐ ││ ────────────────────────────────────────────── │
│ │ 변동 전 (전주) │ ││  ┌────────┬────────┬────────┬────────┐        │
│ │  8-1주.xlsx  ✓ │ ││  │변동 전 │변동 후 │ 증감액 │ 증감률 │        │
│ └────────────────┘ ││  │9,117,124│9,105,633│▼11,491│ -0.13% │        │
│ ┌────────────────┐ ││  └────────┴────────┴────────┴────────┘        │
│ │ 변동 후 (금주) │ ││        st.metric × 4 (delta 자동 색상)         │
│ │  8-2주.xlsx  ✓ │ ││ ────────────────────────────────────────────── │
│ └────────────────┘ ││  ▍변동 후 매출계획 (증감 표시)                 │
│      [⇄ 전후 교체] ││  팀파트          │'26.01│ … │'26.07│  합계     │
│                    ││  ─────────────── ┼──────┼───┼──────┼────────   │
│ ──────────────     ││  [후판]HD현대파트│55,331│ … │60,120│ 402,551   │
│ ⏱ 기간 단위        ││                  │  −   │   │▼2,140│ ▼2,140    │
│  ◉ 월  ○ 분기      ││  [후판]수출파트  │  …   │   │  …   │   …       │
│  ○ 반기 ○ 연간     ││  ───────────────────────────────────────────   │
│                    ││  총계            │      │   │      │           │
│ 📊 측정값          ││                            [⬇ Excel 다운로드]  │
│  ◉ 금액(백만원)    ││ ────────────────────────────────────────────── │
│  ○ 수량(톤)        ││  ▍상세 분석 — 2026년 7월                       │
│  ○ 단가(천원/톤)   ││                                                │
│                    ││  물량효과 ▼x,xxx (△%)  단가효과 ▼x,xxx (△%)   │
│ 🧭 분석 축         ││   └ Base ▼x,xxx  Extra ▼xxx  운임 ▲xx         │
│  [팀파트      ▾]   ││                                                │
│  [+ 2단계 축  ▾]   ││  ┌──────── 워터폴 차트 (Plotly) ───────────┐  │
│                    ││  │ 변동전 ─▼물량─▼Base─▼Extra─▲운임─ 변동후│  │
│ 🔍 표시 옵션       ││  └─────────────────────────────────────────┘  │
│  ☑ 변동 행만       ││                                                │
│  증감표시 [값+% ▾] ││  [기여도 Top10] [신규 행 0] [삭제 행 0]        │
│                    ││   └ st.tabs                                    │
│ ──────────────     ││  ▸ 원본 행 비교 (선택 항목)  ← st.expander    │
│ 🎯 상세 분석 대상  ││                                                │
│  [2026년 7월  ▾]   ││                                                │
└────────────────────┘└────────────────────────────────────────────────┘
```

### 6.1 Streamlit 컴포넌트 매핑

| 화면 요소 | 컴포넌트 | 비고 |
|---|---|---|
| 파일 업로드 2개 | `st.file_uploader` × 2 (sidebar) | `type=["xlsx"]` |
| 전후 교체 | `st.button` + `session_state` 스왑 | |
| 기간 단위 | `st.radio(horizontal=True)` 또는 `st.segmented_control` | 월/분기/반기/연간 |
| 측정값 | `st.radio` | 수량/금액/단가 |
| 분석 축 | `st.selectbox` (2단계는 추가 selectbox) | |
| 요약 카드 | `st.columns(4)` + `st.metric(delta=…)` | delta 색상 자동 |
| 메인 표 | `st.dataframe(Styler, use_container_width=True)` | 증감 색상은 Styler |
| 표 행 선택 | `selection_mode="single-row", on_select="rerun"` | 미지원 시 셀렉트박스로 폴백 |
| 워터폴 차트 | `st.plotly_chart` + `go.Waterfall` | Plotly가 워터폴 기본 제공 |
| 상세 탭 | `st.tabs(["기여도 Top10","신규","삭제"])` | |
| 원본 행 비교 | `st.expander` + `st.dataframe` | |
| 다운로드 | `st.download_button` | xlsx(openpyxl) / csv |
| 진행 표시 | `st.spinner` | 파싱 구간 |

### 6.2 증감 표시 규칙

- **증가 빨강 ▲ / 감소 파랑 ▼ / 무변동 회색 `−`** (국내 재무 관행. Streamlit `st.metric`의 기본 delta 색상은 증가=초록이므로 `delta_color`를 반드시 조정하거나 커스텀 HTML로 표시한다.)
- 셀 표기: 변동 후 값을 크게, 증감을 그 아래 작게 (`값\n▼2,140 (-3.4%)`)
- 금액은 천단위 구분·백만원 정수, 증감률은 소수 둘째 자리
- 변동 있는 셀은 배경 음영으로 강조 → 277행 중 26행을 즉시 식별
- 신규 행은 증감률 자리에 `신규`, 삭제 행은 `삭제` 뱃지

> **Styler 성능 주의**: pandas Styler는 셀 단위로 CSS를 생성해 수천 셀 이상에서 느려진다. 현재 규모(수십 행 × 최대 13열)는 문제없으나, 원본 행 단위 표(277행)에 Styler를 적용할 때는 변동 행만 필터링한 뒤 적용한다.

---

## 7. 기술 아키텍처

### 7.1 기술 스택

| 구분 | 선택 | 사유 |
|---|---|---|
| 실행 | **Streamlit** (로컬, localhost 바인딩) | 업로드·위젯·표가 기본 제공, 프로토타입 속도 |
| 언어 | Python 3.11+ | |
| Excel 파싱 | **openpyxl** (`read_only=True`) | 헤더 5행 구조를 직접 제어해야 하므로 pandas 자동 파싱에 의존하지 않음 |
| 데이터 처리 | **pandas** + numpy | 복합키 merge, groupby 집계 |
| 차트 | **Plotly** (`go.Waterfall`) | 워터폴 차트 기본 제공, 한글 폰트 문제 없음(브라우저 렌더) |
| 표 스타일 | pandas Styler | 증감 색상·음영 |
| 테스트 | pytest | §9 회귀 기준값 자동 검증 |

> **matplotlib 배제**: Windows에서 한글 폰트 설정이 매번 문제가 되고 워터폴을 직접 그려야 한다. Plotly는 둘 다 해결된다.

### 7.2 디렉토리 구조

```
매출계획_변동분석/
├── run.bat                   # 더블클릭 실행 (venv 자동 생성 + streamlit run)
├── requirements.txt
├── app.py                    # Streamlit 엔트리포인트 (UI 조립만)
├── .streamlit/
│   └── config.toml           # 업로드 용량, 주소, 테마
├── core/
│   ├── schema.py             # 컬럼 상수, 차원/측정값 정의, 단위 맵
│   ├── loader.py             # xlsx 파싱, 헤더 행 탐지, 정규화, 날짜 변환
│   ├── differ.py             # 복합키 매칭, 상태 분류, 효과 분해
│   ├── aggregator.py         # 기간(월/분기/반기/연간) · 축 집계
│   └── formatter.py          # Styler, 숫자 포맷, 증감 표기
├── tests/
│   ├── test_loader.py
│   ├── test_differ.py        # 항등식 검증 (잔차 0)
│   └── test_regression.py    # 8-1주 vs 8-2주 실측 기준값
└── samples/
    ├── 8-1주.xlsx
    └── 8-2주.xlsx
```

**계산 로직은 `core/`에 두고 `app.py`는 UI 조립만 한다.** Streamlit에 묶이지 않아야 pytest로 로직을 단독 검증할 수 있고, 나중에 다른 UI로 옮기기도 쉽다.

### 7.3 requirements.txt

```
streamlit>=1.36.0
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.0
plotly>=5.22.0
XlsxWriter>=3.2.0     # 결과 다운로드용
pytest>=8.0.0         # 개발용
```

### 7.4 데이터 처리 파이프라인

```
st.file_uploader → UploadedFile (BytesIO)
   ↓ 파일 바이트 해시 계산 (캐시 키)
@st.cache_data  load_plan(file_bytes) ──────────────┐
   ↓ openpyxl load_workbook(read_only=True)         │ 파일이 같으면
시트 "후판" (없으면 첫 시트 + 경고)                  │ 재파싱 안 함
   ↓ A열 == "변경" 인 행 탐색 → 헤더 행 확정         │
   ↓ 6행부터 순회 → DataFrame                       │
   ↓ 정규화                                          │
   ·문자열: strip, 공백정리, 빈값 → "미지정"         │
   ·숫자: to_numeric(errors="coerce").fillna(0)      │
   ·기준년월: Excel serial → Timestamp → "YYYY-MM"   │
   ·분기/반기: 원본 U·V 우선, 없으면 월에서 파생     │
DataFrame (277 × 정규화 컬럼) ──────────────────────┘
   ↓
@st.cache_data  build_diff(df_before, df_after)
   ↓ 복합키(차원18 + 기준년월) 생성
   ↓ outer merge (indicator=True)
   ↓ 상태 분류: 유지 / 신규 / 삭제 / 값변동
   ↓ 행 단위 효과 분해 (§5.2, §5.3)
diff DataFrame  { 키, 전, 후, Δ수량, Δ금액, 물량효과, Base/Extra/운임 기여, 상태 }
   ↓  ← 여기까지가 캐시. 이후는 위젯 조작마다 재실행되지만 가볍다.
aggregate(diff, 기간단위, 분석축, 측정값)   ← groupby + sum
   ↓ 표시용 단가는 마지막에 Σ금액/Σ수량 재계산
format_table(...)  → Styler
   ↓
st.dataframe / st.metric / st.plotly_chart
```

### 7.5 Streamlit 특성 대응 (설계상 반드시 지킬 것)

Streamlit은 위젯을 건드릴 때마다 **스크립트 전체를 처음부터 다시 실행**한다. 이 특성을 무시하면 축을 바꿀 때마다 Excel을 다시 파싱해 체감 속도가 무너진다.

| 이슈 | 대응 |
|---|---|
| 위젯 조작 시 전체 rerun | 파싱·diff 계산을 `@st.cache_data`로 감싼다. 캐시 키는 **파일 바이트 해시**(UploadedFile 객체는 키로 부적합) |
| 업로드 파일 유실 | 업로드 직후 `getvalue()`로 바이트를 확보해 `st.session_state`에 보관 |
| 전후 교체 버튼 | `session_state`의 두 슬롯을 스왑하고 `st.rerun()` |
| 상세 분석 선택 상태 | `st.session_state["detail_period"]`로 유지 (rerun에도 선택 보존) |
| 캐시 메모리 누수 | `@st.cache_data(max_entries=4, ttl=3600)` |
| DataFrame 캐시 변조 | 캐시 반환값은 수정하지 않는다 (필요 시 `.copy()`) |

### 7.6 설치 및 실행

**선행 조건**: Python 3.11+ 설치. 현재 PC에는 Microsoft Store 스텁만 있으므로 python.org 정식 설치본이 필요하다(설치 시 *Add python.exe to PATH* 체크).

`run.bat` — 더블클릭 한 번으로 가상환경 생성부터 실행까지 처리한다.

```bat
@echo off
cd /d %~dp0
if not exist .venv (
    echo [초기 설정] 가상환경을 생성합니다...
    python -m venv .venv
    .venv\Scripts\python -m pip install -U pip
    .venv\Scripts\pip install -r requirements.txt
)
.venv\Scripts\streamlit run app.py
```

`.streamlit/config.toml`

```toml
[server]
address = "localhost"      # 외부 접속 차단
port = 8501
maxUploadSize = 200        # MB
headless = false           # 실행 시 브라우저 자동 오픈

[browser]
gatherUsageStats = false   # 사용 통계 외부 전송 차단

[theme]
base = "light"
```

> `gatherUsageStats = false`와 `address = "localhost"`는 **사내 데이터 보호를 위한 필수 설정**이다. 기본값으로 두면 사용 통계가 외부로 나가고, 서버가 모든 네트워크 인터페이스에 바인딩된다.

### 7.7 성능 목표

277행 × 55열은 pandas에 부담이 없다. 양식이 커질 것을 감안한 목표:

| 구간 | 목표 |
|---|---|
| 최초 업로드 → 결과 표시 | 5초 이내 (5,000행 기준) |
| 캐시 적중 시 rerun | 300ms 이내 |
| 기간·축 전환 (재파싱 없음) | 500ms 이내 |
| 앱 콜드 스타트 (`streamlit run`) | 10초 이내 |

---

## 8. 예외 처리

| 상황 | 처리 |
|---|---|
| 시트명이 `후판`이 아님 | 첫 번째 시트로 대체하되 경고 표시 |
| 헤더 행(`변경`)을 못 찾음 | 시트 미리보기와 함께 "헤더 행을 선택하세요" UI 제공 |
| 두 파일 컬럼 구성 불일치 | 어떤 컬럼이 다른지 명시하고 분석 중단 |
| 두 파일이 완전히 동일 | "변동 없음" 안내 (오류 아님) |
| 같은 파일을 두 번 업로드 | 파일 해시 비교 후 경고 |
| 복합키 중복 | 경고 배너 + 중복 목록, 임의 합산 금지 |
| 신규/삭제 행 존재 | 별도 섹션으로 분리 표시, 단가효과에서 제외 |
| 수량 0인데 금액 존재 | 단가 계산 시 0 나눗셈 방지, 해당 행 단가 `—` 표기 |
| 기간 범위가 서로 다름 | 합집합으로 표를 만들고 없는 쪽은 0 처리 + 표시 |
| 손상/암호화 파일 | 파싱 예외를 잡아 사용자 메시지로 변환, 앱 중단 금지 |
| 파일 미업로드 상태 | `st.info`로 안내 후 `st.stop()` — 이후 코드 실행 차단 |
| 한쪽 파일만 업로드 | 해당 파일의 요약 정보만 표시, 분석 섹션은 대기 상태 |
| 포트 8501 사용 중 | `run.bat`에서 사용 여부 확인 후 8502로 대체 시도 |
| 브라우저 자동 오픈 실패 | 콘솔에 URL(`http://localhost:8501`)을 안내 문구와 함께 출력 |
| 예상치 못한 예외 | 전역 try/except로 `st.error` + `st.exception` 표시, 앱은 살려둠 |

---

## 9. 테스트 계획

### 9.1 회귀 테스트 (실측 기준값)

제공된 8-1주 / 8-2주 파일로 다음이 재현되어야 통과:

| 검증 항목 | 기대값 |
|---|---|
| 파싱 행 수 | 각 파일 277행 |
| 복합키 유니크 | 277 / 277, 중복 0 |
| 변동 행 수 | 26행 |
| 변동 발생 기간 | 2026-07 단일 월 |
| 2026-01~06 증감 | 전부 0 |
| 2026-07 수량 증감 | 1,075,073 → 1,067,673 (−7,400톤) |
| 2026-07 금액 증감 | 1,430,709.9 → 1,419,218.7 (−11,491.2백만원, −0.80%) |
| 신규/삭제 행 | 0건 |

### 9.2 로직 검증

| 유형 | 항목 |
|---|---|
| 항등식 | 물량효과 + 단가효과 = 금액 증감 (잔차 0) |
| 항등식 | Base + Extra + 운임 기여 = 단가효과 |
| 항등식 | 단가 = Base + Extra + 운임 (원본 검산) |
| 항등식 | 금액 = 수량 × 단가 ÷ 1,000 |
| 집계 | 월 합계 = 분기 합계 = 반기 합계 = 연간 합계 |
| 집계 | 집계 단가가 산술평균이 아닌 가중평균인지 |
| 엣지 | 행 추가/삭제를 인위적으로 만든 파일로 분류 정확성 확인 |
| 엣지 | 변동 전 0 → 증감률 `신규` 표기 (∞/NaN 미노출) |
| UI 스모크 | `st.testing.v1.AppTest`로 앱 기동 → 위젯 전환 → 예외 미발생 확인 |
| 캐시 | 위젯 조작 시 `load_plan` 재호출이 없는지 (호출 카운터로 검증) |

**§9.1은 `tests/test_regression.py`로 자동화한다.** 샘플 파일 2개를 `samples/`에 두고 CI 없이도 `pytest` 한 줄로 언제든 돌릴 수 있게 한다. 로직을 리팩터링할 때 이 테스트가 안전망이 된다.

---

## 10. 개발 마일스톤

| 단계 | 범위 | 산출물 |
|---|---|---|
| **M0. 환경** | Python 3.11 설치, venv, `requirements.txt`, `run.bat`, `.streamlit/config.toml` | `streamlit hello` 정상 실행 |
| **M1. 파서** | `core/schema.py`, `core/loader.py` — 헤더 자동 탐지, 정규화, 날짜 변환 | pytest로 277행 파싱 검증 통과 |
| **M2. 분석 엔진** | `core/differ.py` — 복합키 매칭, 상태 분류, 효과 분해 | **§9.1 회귀 기준값 전부 일치** (UI 없이 pytest로 검증) |
| **M3. 최소 UI** | `app.py` — 업로드 2개, 캐시, 요약 카드, 기본 표 | 브라우저에서 전/후 파일 업로드 → 총계 증감 확인 |
| **M4. 메인 표** | `core/aggregator.py`, `core/formatter.py` — 기간 토글, 축 선택, 증감 Styler | 월/분기/반기/연간 전환 동작 |
| **M5. 상세 분석** | 요약 카드, 효과 분해, Top N, Plotly 워터폴, 탭 | 기간 선택 → 상세 패널 |
| **M6. 마감** | 예외 처리, `session_state` 정리, 다운로드, README | v1.0 릴리스 |

**M2까지는 UI 없이 진행한다.** 계산 로직이 §9.1 기준값을 재현하는지 pytest로 먼저 확정한 뒤 화면을 붙여야, 나중에 숫자가 틀렸을 때 원인이 로직인지 렌더링인지 헤매지 않는다.

---

## 11. 향후 확장 (v2 이후)

- **3주차 이상 시계열 비교** — 계획 변경 추이 그래프
- **손익 변동 분석** — 영업이익·총원가·경상이익 증감 및 마진율 변화
- **수출 환율효과 분해** — 환율(W)·USD 단가(AD) 변동을 분리
- **코멘트/사유 입력** — 변동 항목에 담당자 설명을 붙여 보고서로 출력
- **PPT/보고서 자동 생성** — 회의자료_대분류 기준 요약 슬라이드
- **타 사업부 양식 프로파일** — 양식별 컬럼 매핑을 JSON으로 정의해 확장
- **사내 공유 배포** — 사내 서버에 Streamlit 상시 구동(`--server.address 0.0.0.0` + 사내망 한정) 하여 팀원이 URL만으로 접속. 외부 클라우드(Streamlit Community Cloud 등)는 사내 데이터 반출에 해당하므로 배제한다.
