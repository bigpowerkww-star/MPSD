# 단계별 실행 프롬프트

[PRD_매출계획_변동분석.md](PRD_매출계획_변동분석.md)를 4단계로 나눈 실행 프롬프트다. 각 단계의 코드 블록을 **그대로 복사해서 Claude Code에 붙여넣으면** 된다.

## 사용 방법

- **순서대로** 실행한다. 각 단계는 이전 단계의 산출물을 전제로 한다.
- 각 단계는 **완료 조건**을 만족해야 다음으로 넘어간다. 특히 2단계의 회귀 테스트가 통과하지 않으면 3단계로 가지 말 것.
- 각 프롬프트 마지막에 CLAUDE.md 갱신이 포함되어 있다. 새 세션에서 다음 단계를 시작해도 맥락이 이어진다.
- 단계마다 새 세션을 여는 것을 권장한다. 컨텍스트가 깨끗한 편이 낫고, CLAUDE.md가 인계 역할을 한다.

| 단계 | 범위 | PRD 마일스톤 | 완료 신호 |
|---|---|---|---|
| 1 | 환경 + 파서 뼈대 | M0, M1 | `pytest tests/test_loader.py` 통과, 앱 기동 |
| 2 | 분석 엔진 + 기능 연결 | M2, M3, M4 | **회귀 기준값 재현** |
| 3 | UI 디자인 | M5 | 화면 완성 |
| 4 | 검토 | M6 | `REVIEW.md` 산출 |

---

## 1단계 — 기본 뼈대 작성

```
PRD_매출계획_변동분석.md와 CLAUDE.md를 먼저 읽고, 1단계(M0 환경 + M1 파서)를 구현해줘.

[선행 확인]
이 PC의 `python`은 Microsoft Store 스텁이라 조용히 실패한다. 먼저 `python --version`으로
실제 Python 3.11+ 가 설치되어 있는지 확인하고, 스텁이면 그 사실을 알려주고 멈춰줘.
(python.org 정식 설치본 + "Add python.exe to PATH" 체크 필요)

[만들 것]
1. requirements.txt — PRD 7.3 그대로
2. run.bat — PRD 7.6 그대로 (venv 자동 생성 + 패키지 설치 + streamlit run)
3. .streamlit/config.toml — PRD 7.6 그대로.
   address="localhost", gatherUsageStats=false 는 사내 데이터 보호용 필수 설정이니 빠뜨리지 말 것
4. .gitignore — .venv, .env, __pycache__, samples 내 실데이터
5. samples/ — Downloads의 샘플 2개를 복사해 8-1주.xlsx / 8-2주.xlsx 로 저장
   원본: C:\Users\User\Downloads\후판) □□영업본부_'26년 실행계획_신규양식_실습용 더미데이터_8-{1,2}주.xlsx
6. core/schema.py
   - 차원 18개, 시점 3개, 측정값 컬럼명을 상수로 정의 (PRD 3.2)
   - 컬럼별 단위 맵 (톤 / 백만원 / 천원/톤 / $/톤 / 백만$)
   - 복합키 구성 컬럼 리스트 (차원 18 + 기준년월)
   - 분석 축으로 쓸 수 있는 차원 목록
7. core/loader.py — 이 단계의 핵심
   - openpyxl로 파싱 (read_only=True). pandas 자동 파싱에 맡기지 말 것
   - 헤더 행은 행 번호 하드코딩 금지. A열 값이 "변경"인 행을 탐색해서 찾는다
   - 시트는 "후판" 우선, 없으면 첫 시트 + 경고
   - 정규화: 문자열 strip/공백정리/빈값→"미지정", 숫자 to_numeric(errors="coerce").fillna(0),
     기준년월 Excel serial → Timestamp → "YYYY-MM", 분기/반기는 원본 U·V 우선 없으면 월에서 파생
   - 반환: 정규화된 DataFrame + 메타정보(시트명, 행수, 기간범위, 경고 목록)
8. app.py — 이 단계에서는 최소한만
   - layout="wide", 사이드바에 file_uploader 2개(변동 전 / 변동 후)
   - 업로드된 파일의 파싱 결과 요약(시트명, 행수, 기간 범위, 컬럼수)만 표시
   - 파일 미업로드 시 st.info 후 st.stop()
   - @st.cache_data 적용. 캐시 키는 UploadedFile 객체가 아니라 파일 바이트 해시를 쓸 것
9. tests/test_loader.py
   - 샘플 2개 각각 277행으로 파싱되는지
   - 복합키(차원18+기준년월)가 277개 전부 유니크한지
   - 기간 범위가 2026-01 ~ 2026-07 로 인식되는지
   - 항등식 검산: 단가 == Base+Extra+운임, 금액 == (Basex수량+Extrax수량+운임x수량)/1000
     (부동소수점이므로 상대오차 허용)

[하지 말 것]
- 이번 단계에서 diff/집계/차트/스타일링은 건드리지 않는다
- 계산 로직을 app.py에 넣지 않는다. core/ 에만 둔다

[완료 조건]
- pytest tests/test_loader.py 전부 통과
- streamlit run app.py 로 앱이 뜨고, 샘플 2개 업로드 시 요약 정보가 표시됨

[마무리]
끝나면 CLAUDE.md를 실제 진행 상태에 맞게 갱신해줘.
- "프로젝트 상태"를 구현 전 → 1단계 완료로
- "개발 환경"의 계획된 명령어를 실제 동작하는 명령어로 확정
- loader가 실제로 어떻게 헤더를 찾고 무엇을 반환하는지, 구현하며 알게 된 함정을 추가
- 이미 적혀 있는 내용을 반복하지 말고, 틀려진 부분만 고칠 것
```

---

## 2단계 — 주요 기능

```
PRD_매출계획_변동분석.md와 CLAUDE.md를 읽고, 2단계(M2 분석 엔진 + M3/M4 기능 연결)를 구현해줘.

이 단계가 프로젝트의 핵심이다. 화면을 예쁘게 만드는 건 3단계에서 하고,
지금은 숫자가 정확한지에만 집중해줘.

[만들 것]

1. core/differ.py — 변동 분석 엔진
   - 복합키(차원18 + 기준년월)로 전/후 DataFrame을 outer merge (indicator 사용)
   - 상태 분류: 유지 / 신규추가 / 삭제 / 값변동 (FR-202)
     · 신규는 변동 전 0, 삭제는 변동 후 0으로 채워 합계 정합성 유지 (FR-203)
   - 행 단위 효과 분해 (PRD 5.2 / 5.3):
       물량효과 = (Q1 - Q0) * P0 / 1000
       단가효과 = Q1 * (P1 - P0) / 1000
       Base기여  = Q1 * (Base1 - Base0) / 1000
       Extra기여 = Q1 * (Extra1 - Extra0) / 1000
       운임기여  = Q1 * (운임1 - 운임0) / 1000
   - 신규/삭제 행은 단가효과로 배분하지 않는다. 전액 물량효과로 잡고 상태로 구분한다 (PRD 5.4)
   - 부동소수점: 상대오차 1e-9 이하는 변동 없음으로 간주 (FR-205)
   - 복합키 중복이 있으면 합산하지 말고 중복 목록을 반환해 호출자가 경고하게 할 것 (FR-207)
   - 반환: 행 단위 diff DataFrame (키 + 전/후 값 + 각 효과 + 상태)

2. core/aggregator.py — 집계
   - 기간 단위(월/분기/반기/연간) × 분석 축(팀파트 등) 으로 diff를 groupby+sum
   - 분기/반기는 원본 U·V 우선, 없으면 월에서 파생 (FR-302)
   - 데이터에 존재하는 기간만 열로 만든다. 빈 기간 열 생성 금지 (FR-304)
   - **표시용 단가는 반드시 마지막에 Σ금액/Σ수량*1000 으로 재계산** (FR-306)
     행 단가의 산술평균을 내면 틀린 값이 나온다
   - **효과는 행 단위로 먼저 계산하고 나중에 합산한다** (PRD 5.5)
     집계 평균단가로 효과를 계산하면 구성비 변화(mix)가 단가효과로 잘못 섞인다
   - 증감률 분모 0(신규)일 때 inf/NaN 대신 "신규" 표기용 마커를 반환 (FR-206)
   - 2단계 축(예: 팀파트 → 고객그룹) 지원 (FR-406)
   - 상세 분석용: 특정 기간의 요약 + 효과 분해 + 기여도 Top N + 신규/삭제 목록

3. tests/test_differ.py — 항등식 검증 (PRD 9.2)
   - 물량효과 + 단가효과 == 금액 증감 (잔차 0)
   - Base + Extra + 운임 기여 == 단가효과
   - 월 합계 == 분기 합계 == 반기 합계 == 연간 합계
   - 집계 단가가 산술평균이 아니라 가중평균인지
   - 행을 인위적으로 추가/삭제한 픽스처로 신규/삭제 분류 정확성
   - 변동 전 0일 때 inf/NaN이 노출되지 않는지

4. tests/test_regression.py — PRD 9.1 실측 기준값
   samples/8-1주.xlsx → 8-2주.xlsx 로 아래가 정확히 재현되어야 한다:
   - 파싱 행 수: 각 277
   - 복합키 유니크: 277/277
   - 변동 행 수: 26, 전부 2026-07
   - 2026-01~06 증감: 전부 0
   - 2026-07 수량: 1,075,073 → 1,067,673 (-7,400톤)
   - 2026-07 금액: 1,430,709.9 → 1,419,218.7 (-11,491.2백만원, -0.80%)
   - 신규/삭제 행: 0건

5. app.py 연결 (기능 위주, 디자인은 3단계)
   - 사이드바: 기간 단위 라디오(월/분기/반기/연간), 측정값 라디오(수량/금액/단가),
     분석 축 셀렉트박스, 변동 행만 필터, 상세 분석 대상 셀렉트박스
   - 전후 교체 버튼 (session_state 스왑 + st.rerun)
   - 메인: 총계 요약 metric, 기본 st.dataframe 표, 상세 분석 수치 출력
   - diff 계산도 @st.cache_data로 감쌀 것. 위젯을 건드릴 때마다 Excel을 다시 파면 안 된다
   - 업로드 파일은 getvalue()로 바이트를 확보해 session_state에 보관

[중요]
회귀 테스트가 통과하지 않으면 그 상태로 멈추고 원인을 보고해줘.
숫자가 안 맞는데 UI를 얹으면 나중에 로직 문제인지 렌더링 문제인지 구분이 안 된다.

[완료 조건]
- pytest 전체 통과, 특히 test_regression.py 기준값 전부 일치
- 앱에서 기간/축/측정값을 바꾸면 표가 즉시 바뀌고, 재파싱이 일어나지 않음

[마무리]
CLAUDE.md를 갱신해줘.
- 진행 상태를 2단계 완료로
- 실제 함수 시그니처와 모듈 간 데이터 흐름(loader → differ → aggregator)을 간결히
- 구현하며 실제로 부딪힌 함정을 "자주 틀리는 지점"에 추가 (추측 말고 실제 겪은 것만)
- 회귀 테스트 실행 명령을 확정된 형태로
```

---

## 3단계 — UI 디자인

```
PRD_매출계획_변동분석.md 6장(화면 설계)과 CLAUDE.md를 읽고, 3단계 UI를 구현해줘.

계산 로직(core/)은 이미 검증됐다. 건드리지 말고, 표시 계층만 작업해줘.
숫자를 바꿔야 할 것 같으면 먼저 물어봐줘.

[만들 것]

1. core/formatter.py — 표시 계층 분리
   - 숫자 포맷: 금액 천단위 구분 백만원 정수, 수량 톤, 단가 소수1자리, 증감률 소수2자리
   - 증감 표기: 증가 "▲", 감소 "▼", 무변동 "−", 신규/삭제는 뱃지
   - pandas Styler로 셀 색상 + 변동 셀 배경 음영

2. 색상 규칙 (PRD 6.2) — 국내 재무 관행을 따른다
   - 증가 = 빨강 ▲ / 감소 = 파랑 ▼ / 무변동 = 회색 −
   - **st.metric의 기본 delta 색상은 증가=초록이라 반대다.**
     delta_color="inverse"로 뒤집거나 커스텀 표시로 처리할 것. 이거 빠뜨리기 쉽다

3. 레이아웃 (PRD 6장 와이어프레임)
   - layout="wide", 조작 위젯은 전부 사이드바, 결과는 메인
   - 사이드바 순서: 파일 업로드 2개 + 교체 버튼 → 기간 단위 → 측정값 → 분석 축
     → 표시 옵션 → 상세 분석 대상
   - 메인 순서: 헤더(파일 라벨/행수/기간) → 요약 metric 4개 → 메인 표 → 상세 분석

4. 메인 표 (FR-401~409)
   - 행=분석 축, 열=선택한 기간 + 합계
   - 각 셀에 변동 후 값 + 증감을 함께 표시
   - 증감 표시 모드 전환: 절대값 / 증감률 / 둘 다
   - 총계 행, 헤더/1열 고정
   - Excel/CSV 다운로드 버튼
   - Styler는 셀 단위 CSS를 만들어 느려진다. 원본 행 표(277행)에는
     변동 행만 필터링한 뒤 적용할 것

5. 상세 분석 패널 (FR-501~508)
   - 요약 카드(st.columns + st.metric): 변동 전 / 후 / 증감액 / 증감률 / 변동 행 수
   - 효과 분해 표시: 물량효과 · 단가효과 + Base/Extra/운임 세부
   - Plotly go.Waterfall: 변동전 → 물량효과 → Base → Extra → 운임 → 변동후
     (matplotlib 쓰지 말 것. Windows 한글 폰트 문제 + 워터폴 직접 그려야 함)
   - st.tabs: 기여도 Top10 / 신규 행 / 삭제 행
   - st.expander: 선택 항목의 원본 행 전/후 비교, 변동 컬럼 하이라이트
   - 연간 선택 시 월별 증감 추이 함께 표시
   - 기간 선택은 표 행 클릭(selection_mode="single-row", on_select="rerun")과
     사이드바 셀렉트박스 둘 다 지원. 버전 차이가 있으니 셀렉트박스를 항상 동작하는 폴백으로

6. 예외 UI (PRD 8장)
   - 파일 미업로드 → st.info + st.stop()
   - 한쪽만 업로드 → 그 파일 요약만, 분석 섹션은 대기
   - 컬럼 구성 불일치 → 어떤 컬럼이 다른지 명시하고 중단
   - 두 파일 동일 → "변동 없음" 안내 (오류 아님)
   - 같은 파일 두 번 → 해시 비교 후 경고
   - 복합키 중복 → 경고 배너 + 목록
   - 전역 try/except → st.error + st.exception, 앱은 살려둘 것

[완료 조건]
- 샘플 2개로 전체 화면이 PRD 6장 와이어프레임대로 동작
- 기간/축/측정값 전환이 500ms 이내로 체감
- 2026년 7월 상세 분석에서 워터폴이 -11,491.2백만원을 물량/단가로 분해해 보여줌

[마무리]
CLAUDE.md를 갱신해줘. UI 계층 구조와, 3단계에서 실제로 부딪힌 Streamlit 제약을 추가.
```

---

## 4단계 — 검토

```
구현이 끝났다. PRD_매출계획_변동분석.md 대비 실제 구현을 처음부터 끝까지 검토해줘.
"아마 될 것이다"가 아니라 실제로 돌려보고 확인해줘.

[1. PRD 요구사항 대조]
FR-101 ~ FR-508 전부를 코드에서 확인하고 표로 정리해줘.
컬럼: ID / 요구사항 / 상태(구현·부분·미구현) / 근거 파일:라인 / 비고
- "구현"으로 표시하려면 실제 코드 위치를 짚을 수 있어야 한다
- Must인데 미구현/부분인 항목은 따로 모아서 강조

[2. 숫자 정확성 재검증]
- pytest 전체 실행. 실패가 있으면 출력 그대로 보여줄 것
- PRD 9.1 회귀 기준값 8개 항목 실측 대조 (특히 2026-07 -11,491.2백만원)
- 항등식이 실제 데이터에서 성립하는지 직접 계산해 확인:
    물량효과 + 단가효과 == 금액 증감 (잔차 0인지)
    Base + Extra + 운임 기여 == 단가효과
    월합 == 분기합 == 반기합 == 연간합
- 집계 단가가 가중평균인지 실제 값으로 확인 (산술평균과 결과가 다른 케이스를 만들어 비교)

[3. 버그 헌팅 — 아래를 실제로 시도해보고 결과를 보고]
- 전/후 파일을 반대로 업로드
- 같은 파일을 양쪽에 업로드
- 한쪽만 업로드한 상태에서 위젯 조작
- 행을 인위적으로 삭제/추가한 파일로 신규·삭제 분류 확인
- 수량 0, 금액 0인 행이 섞였을 때 0 나눗셈
- 변동 전 0 → 증감률에 inf/NaN이 화면에 노출되는지
- 모든 기간 단위 × 모든 분석 축 조합을 순회하며 예외 발생 여부
- 파일 업로드 후 위젯을 여러 번 조작했을 때 재파싱이 일어나는지
  (loader에 호출 카운터를 심어 확인)
- session_state가 rerun 사이에 깨지지 않는지

[4. 코드 품질]
- core/ 에 Streamlit 의존이 새어 들어가지 않았는지 (import streamlit 검색)
- app.py에 계산 로직이 섞이지 않았는지
- 캐시 반환값을 수정하는 코드가 없는지 (DataFrame 변조 → 캐시 오염)
- 매직넘버 / 하드코딩된 행·열 번호
- 죽은 코드, 중복 로직

[5. 설정 확인]
- .streamlit/config.toml 의 address="localhost", gatherUsageStats=false 가 실제로 들어 있는지
- .gitignore 가 .env 와 샘플 실데이터를 막고 있는지

[산출물]
REVIEW.md 로 정리해줘. 구성:
  1) 요약 — 전체 판정과 핵심 이슈 3줄
  2) PRD 대조표
  3) 발견된 문제 — 심각도(치명/중대/경미)별로, 재현 방법과 원인 위치 포함
  4) 수정 제안 — 우선순위 순
  5) 검증 통과 항목

[중요]
이번 단계에서는 **문제를 찾아서 보고만** 해줘. 코드는 아직 고치지 말고,
수정할 항목을 내가 고른 다음에 진행하자.
문제가 없으면 없다고 해줘. 억지로 만들어내지 말 것.

[마무리]
CLAUDE.md를 최종 갱신해줘.
- 진행 상태를 v1.0 완성(또는 미해결 이슈 있음)으로
- 실제 확정된 실행/테스트 명령어
- 검토에서 드러난, 앞으로도 계속 주의해야 할 함정
- 이미 해결된 과거 이슈는 CLAUDE.md에서 덜어낼 것
```

---

## 참고

- 2단계 회귀 테스트가 통과하지 않으면 **3단계로 넘어가지 말 것.** PRD가 이 순서를 요구하는 이유는, 숫자가 틀린 상태에서 화면을 얹으면 로직 문제인지 렌더링 문제인지 구분이 안 되기 때문이다.
- 4단계는 검토 전용이다. 수정까지 한 번에 시키면 무엇이 왜 바뀌었는지 추적이 어려워진다.
- 각 단계 프롬프트의 완료 조건은 PRD 10장 마일스톤의 산출물과 대응한다.
