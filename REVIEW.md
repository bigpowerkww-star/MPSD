# 구현 검토 보고서

| 항목 | 내용 |
|---|---|
| 대상 | 매출 실행계획 주간 변동 분석 앱 (1~3단계 구현분) |
| 기준 문서 | [PRD_매출계획_변동분석.md](PRD_매출계획_변동분석.md) FR-101 ~ FR-508, 8장, 9장 |
| 검토일 | 2026-09-01 |
| 검토 방식 | pytest 실행 + 실데이터 직접 계산 검증 + 시나리오 수동 실행 + 브라우저 확인 + 코드 정독 |
| **수정 상태** | **발견된 7건 전부 수정 완료** (2026-09-01). 테스트 **223건** 통과 |

---

## 1. 요약

**숫자는 신뢰할 수 있다.** PRD 9.1 회귀 기준값 8개 항목이 전부 재현되고, 효과 분해 항등식의 최대 잔차는 `5.8e-12`다. 96개 조합(기간 4 × 축 8 × 측정값 3)을 전부 렌더해도 예외·inf·NaN이 나오지 않는다.

초기 검토에서 **치명 0건, 중대 1건, 경미 6건**을 발견했고 **전부 수정했다.** 수정 후 회귀 검증에서 기준 숫자(총계 9,105,633.2 / 7월 −11,491.2 / 잔차 5.8e-12)는 그대로다.

| 등급 | 건수 | 상태 |
|---|---|---|
| 치명 | 0 | — |
| 중대 | 1 | ✅ 수정 완료 |
| 경미 | 6 | ✅ 전부 수정 완료 |

**Must 요구사항 32건 전부 충족.** FR-408(총계 행 고정)은 `st.dataframe`이 임의 행 고정을 지원하지 않아, 총계를 **첫 행**으로 올려 첫 화면에서 바로 보이도록 해결했다.

---

## 2. PRD 요구사항 대조

### 2.1 파일 업로드 (FR-100)

| ID | 요구사항 | 상태 | 근거 |
|---|---|---|---|
| FR-101 | 드롭존 2개 | 구현 | [app.py:162](app.py:162) `capture_upload` × 2 |
| FR-102 | 헤더 행 자동 탐지 | 구현 | [core/loader.py:112](core/loader.py:112) `_find_header_row` — A열 `변경` 탐색 |
| FR-103 | 컬럼 구성 불일치 검증 + 차이 명시 | 구현 | [app.py:271](app.py:271) 차이 컬럼 목록 표시 후 `st.stop()` |
| FR-104 | 파일명에서 **주차 정보 추출**해 라벨 표시 | **부분** | 파일명 전체를 그대로 표시([app.py:283](app.py:283)). `8-1주 → 8-2주` 파싱 없음 |
| FR-105 | 전/후 스왑 버튼 | 구현 | [app.py:199](app.py:199) `swap_slots` + `st.rerun()` |
| FR-106 | 진행 상태 + 인식 결과 요약 | 구현 | `st.spinner` + 행수/기간/시트/헤더행 캡션 |
| FR-107 | 메모리만 처리, localhost 바인딩 | 구현 | 디스크 기록 없음 · [.streamlit/config.toml](.streamlit/config.toml) `address="localhost"` |
| FR-108 | 캐시로 재파싱 방지 | 구현 | [app.py:57](app.py:57) `@st.cache_data`, 키=파일 바이트. 호출 카운터 테스트로 검증 |

### 2.2 행 매칭 및 증감 산출 (FR-200)

| ID | 요구사항 | 상태 | 근거 |
|---|---|---|---|
| FR-201 | 정규화 | 구현 | [core/loader.py:150](core/loader.py:150) `_normalize` |
| FR-202 | 유지/신규/삭제/값변동 4분류 | 구현 | [core/differ.py:71](core/differ.py:71) `build_diff` |
| FR-203 | 신규 0 / 삭제 0 채움 | 구현 | `_assemble` — outer merge 후 `fillna(0.0)` |
| FR-204 | 수량·금액·단가 **및 손익 항목** 증감 | **부분** | 영업이익·손익·총원가는 **계산되지만 화면에서 선택 불가** (`schema.MEASURES` = 금액/수량/단가) |
| FR-205 | 상대오차 1e-9 이하 무시 | 구현 | `_has_material_change` — `np.isclose(rtol=1e-9)` |
| FR-206 | 0분모 시 `신규` 표기, inf/NaN 금지 | 구현 | [core/formatter.py:78](core/formatter.py:78) `fmt_pct` · 96조합 전수 검사 통과 |
| FR-207 | 중복 키 경고, 합산 금지 | 구현 | `_collect_duplicates` → 제외 후 목록 반환, [app.py:311](app.py:311) 배너 |

### 2.3 기간 축 (FR-300)

| ID | 요구사항 | 상태 | 근거 |
|---|---|---|---|
| FR-301 | 월/분기/반기/연간 토글 | 구현 | `st.radio(schema.PERIOD_UNITS)` |
| FR-302 | 원본 U·V 우선, 없으면 파생 | 구현+ | [core/loader.py](core/loader.py) `_coerce_period_part` — **원본과 기준년월 대조 검증 추가** (3.3 참조) |
| FR-303 | 축 전환 시 재파싱 없이 재구성 | 구현 | 캐시 검증 테스트 통과 |
| FR-304 | 존재하는 기간만 열 생성 | 구현 | [core/aggregator.py:88](core/aggregator.py:88) `period_labels` |
| FR-305 | 측정값 전환 | 구현 | `st.radio(schema.MEASURES)` |
| FR-306 | 단가 가중평균 | 구현 | `_finalize` — 산술평균과 최대 84.3천원/톤 차이 발생 확인 |

### 2.4 메인 표 (FR-400)

| ID | 요구사항 | 상태 | 근거 |
|---|---|---|---|
| FR-401 | 변동 후 기준 | 구현 | `Matrix.after` |
| FR-402 | 행 축 선택 | 구현+ | PRD 6종 + 공장·정품구분 = 8종 |
| FR-403 | 열 축 + 합계 | 구현 | 기간 + `합계` 열 |
| FR-404 | 값 + 증감 함께, 방향별 색 | 구현 | [core/formatter.py:156](core/formatter.py:156) `style_matrix` — 증가 빨강 ▲ / 감소 파랑 ▼ / 무변동 회색 − |
| FR-405 | 표시 모드 전환 | 구현 | `DISPLAY_MODES` 4종 |
| FR-406 | 2단계 계층 축 | 구현 | `build_matrix(second_axis=…)` |
| FR-407 | 변동 있는 행만 필터 | 구현 | `changed_only` |
| FR-408 | **총계 행 고정**, 헤더/1열 고정 | 구현 | 헤더·인덱스는 `st.dataframe` 기본 고정. 총계는 **첫 행**으로 배치 — `build_matrix` 가 재정렬 |
| FR-409 | Excel/CSV 다운로드 | 구현 | [app.py:398](app.py:398) · CSV는 BOM 포함(한글 깨짐 방지) |

### 2.5 상세 분석 (FR-500)

| ID | 요구사항 | 상태 | 근거 |
|---|---|---|---|
| FR-501 | 요약 카드 | 구현 | 변동 전/후/증감/증감률 metric + 행 상태 캡션 |
| FR-502 | 물량·단가 효과 및 비중 | 구현 | [app.py:139](app.py:139) `colored_stat` — 비중 % 표기 |
| FR-503 | Base/Extra/운임 세부 | 구현 | 단가효과 내역 라인 |
| FR-504 | 워터폴 차트 | 구현 | [app.py:105](app.py:105) `render_waterfall` — Plotly `go.Waterfall` |
| FR-505 | 기여도 Top N (증가/감소 각각) | 구현 | `_trim_contributors` (기본 10) |
| FR-506 | 신규/삭제 행 목록 | 구현 | `st.tabs` |
| FR-507 | 원본 행 전/후 비교 + 변동 하이라이트 | 구현 | `rows_for_axis(axes, axis_label)` — 표와 동일한 라벨로 대조 (3.1 수정) |
| FR-508 | 연간 선택 시 월별 추이 | 구현+ | 월 이외 모든 단위에서 표시 |

### 2.6 예외 처리 (PRD 8장)

| 상황 | 상태 | 비고 |
|---|---|---|
| 시트명 다름 | 구현 | 첫 시트 대체 + 경고 |
| **헤더 행 못 찾음** | **부분** | 에러 메시지만. PRD의 "시트 미리보기 + 헤더 행 선택 UI" 없음 |
| 컬럼 불일치 | 구현 | 차이 컬럼 명시 후 중단 |
| 두 파일 동일 | 구현 | "변동 없음" 안내 (오류 아님) |
| 같은 파일 두 번 | 구현 | 바이트 비교 후 경고 |
| 복합키 중복 | 구현 | 배너 + 목록, 합산 안 함 |
| 신규/삭제 행 | 구현 | 별도 탭, 단가효과에서 제외 |
| 수량 0 | 구현 | NaN 반환 후 `—` 표기. inf 미발생 확인 |
| 기간 범위 다름 | 구현 | 합집합 처리 + **한쪽에만 있는 기간을 배너로 안내** (3.2-C 수정) |
| 손상/암호화 파일 | 구현 | `PlanLoadError` → `st.error` |
| 파일 미업로드 / 한쪽만 | 구현 | `st.info` + `st.stop()` |
| **포트 8501 사용 중** | **미구현** | [run.bat](run.bat)에 대체 포트 로직 없음 |
| 예상치 못한 예외 | 구현 | `st.error` + `st.exception`, 앱 유지 |

**집계**: Must 32건 **전부 충족**. 전체 44개 항목 중 구현 39 · 부분 3 · 미구현 2 (부분·미구현은 전부 Should/Could 또는 편의 기능).

---

## 3. 발견된 문제 — 전부 수정 완료

### 3.1 중대 ✅ 수정됨 — 2단계 축 사용 시 드릴다운이 1단계만 필터링

**위치**: [app.py:527](app.py:527)

```python
drill = rows_for_axis(diff.rows, period_unit, detail_label,
                      axis, axis_value.split(" / ")[0])   # ← 2단계 값 버림
```

메인 표의 행 인덱스는 2단계 축을 켜면 `"팀파트 / 고객그룹"` 형태로 합쳐진다. 드릴다운은 `" / "`로 잘라 **앞부분만** 필터에 넘기므로, 뒷부분 조건이 사라진다.

**재현**
1. 분석 축 1단계 `팀파트`, 2단계 `비고` 선택
2. 기간 `2026-07`
3. `[후판]HD현대파트 / 기타` 행 클릭

**결과**: 원본 행 비교에 **5행**이 뜬다 (정답 1행). `[후판]HD현대파트`의 비고 4종이 전부 섞여 나온다.

**영향**: 잘못된 행을 근거로 원인을 판단하게 된다. 합계·차트 숫자는 영향 없고 드릴다운 표시만 틀린다.

**수정 내용**: 문자열을 쪼개는 대신 **라벨을 다시 계산해서 비교**하도록 바꿨다.

```python
# core/aggregator.py
def rows_for_axis(rows, period_unit, period_label, axes, axis_label, changed_only=True):
    labels = _axis_index(rows, list(axes))     # 표가 쓰는 것과 같은 함수
    subset = rows[(periods == period_label) & (labels == str(axis_label))]
```

`Matrix`에 `axes`를 실어 `app.py`가 축 목록을 그대로 넘긴다. 구분자로 자르지 않으므로 축 값에 `/`가 들어가도(`후판_건설/철구`, `후판_유통/기타` — 실제로 존재) 안전하다.

**검증**: 재현 케이스가 **5행 → 1행**(정답)으로 바뀌었다. `tests/test_drilldown.py`에 축 조합 5종 전수 대조 + `/` 포함 값 + 셀 금액 일치 테스트를 추가했다.

---

### 3.2 경미 (6건 전부 ✅ 수정됨)

**A. 축 값이 문자열 `"총계"`면 합계가 덮어써진다** ✅
*문제*: `wide.loc[GRAND_TOTAL_LABEL] = grand` — 축에 `총계`라는 값이 있으면 그 행이 덮이고 합계가 틀어진다(실험으로 확인).
*수정*: `_unique_total_label()` 이 축 값과 겹치지 않는 라벨을 고른다(`총계` → `총계 (전체)` → …). `Matrix.total_label` 로 노출하고 `app.py`·`formatter` 가 이를 참조한다.
*검증*: 축 값 5개를 `총계`로 바꿔도 원래 값이 살아 있고 합계가 정확하다.

**B. 총계 행이 스크롤에 고정되지 않는다 (FR-408)** ✅
*문제*: `st.dataframe`은 헤더와 인덱스 열만 고정하며 임의 행 고정 기능이 없다. 스크롤이 생기면 총계가 시야에서 사라진다.
*수정*: `build_matrix` 가 총계를 **첫 행**으로 재정렬한다(다섯 프레임 모두). 첫 화면에서 바로 보이고, 페이지 상단 요약 metric 이 총계를 항상 표시하므로 실질적으로 해소된다.
*한계*: 플랫폼상 진짜 sticky row 는 불가능하다. HTML 표로 바꾸면 가능하지만 행 클릭(FR-507)을 잃는다 — 그쪽이 더 손해라 판단했다.

**C. 기간이 한쪽 파일에만 있어도 그냥 "삭제"로 표시된다** ✅
*수정*: 두 파일의 `periods` 를 비교해 한쪽에만 있는 기간을 `st.info` 배너로 안내한다. 어느 쪽에만 있는지, 신규로 잡히는지 삭제로 잡히는지 명시한다.

**D. 월별 추이 막대가 항상 파랑** ✅
*수정*: `st.bar_chart` → Plotly `go.Bar` 로 교체하고 `marker_color` 를 값별로 준다(`render_trend`). 증가 빨강 / 감소 파랑 / 0 회색.
*검증*: 연간 선택 시 7월만 파랑, 나머지 회색으로 그려지는 것을 브라우저에서 확인.

**E. 기여도 Top N이 잘렸는지 알 수 없다** ✅
*수정*: `PeriodDetail` 에 `contributor_total` 과 `contributors_truncated` 추가. 잘렸으면 "변동이 있는 N개 중 상위 M개만 표시했습니다. 합계가 전체 증감과 다를 수 있습니다.", 아니면 "N개 전부 표시했습니다." 를 캡션으로 띄운다.

**F. `-0.0` 표기와 사용하지 않는 변수** ✅
*수정*: `fmt_number` 가 반올림 후 0이 되는 음수의 부호를 떼어낸다(`-0.04` → `0.0`). 진짜 음수(`-1.5`, `-1,234`)는 부호를 유지한다. 미사용 `event` 변수 제거.

---

## 4. 검증 통과 항목

### 4.1 숫자 정확성

| 검증 | 결과 |
|---|---|
| pytest 전체 | **223건 전부 통과** (35초) |
| 물량효과 + 단가효과 == 금액 증감 | 최대 잔차 **5.826e-12** |
| Base + Extra + 운임 기여 == 단가효과 | 최대 잔차 **0** |
| 월 == 분기 == 반기 == 연간 합계 | 4개 단위 모두 **9,105,633.20** |
| 축별 합계 == 총계 | 8개 축 전부 일치 |
| 집계 단가 == Σ금액/Σ수량×1000 | `rtol=1e-9` 일치 |
| 가중평균 ≠ 산술평균 | 최대 **84.3 천원/톤** 차이 — 가중평균이 실제로 동작 |

### 4.2 PRD 9.1 회귀 기준값

| 항목 | 기대 | 실측 |
|---|---|---|
| 파싱 행 수 | 각 277 | 277 / 277 ✓ |
| 복합키 유니크 | 277 | 277 ✓ |
| 변동 행 수 | 26 | 26 ✓ |
| 변동 기간 | 2026-07 단독 | 2026-07 ✓ |
| 1~6월 증감 | 0 | 0 ✓ |
| 7월 수량 | 1,075,073 → 1,067,673 | 일치 ✓ |
| 7월 금액 | 1,430,709.9 → 1,419,218.7 (−0.80%) | 일치 ✓ |
| 신규/삭제 | 0 | 0 ✓ |

7월 분해: **물량효과 −10,058.1 (87.5%) / 단가효과 −1,433.1 (12.5%, 전액 Base)**

### 4.3 시나리오 실행

| 시나리오 | 결과 |
|---|---|
| 전/후 반대 업로드 | 스왑 버튼 동작 |
| 같은 파일 양쪽 업로드 | 경고 표시, 분석은 진행 |
| 한쪽만 업로드 | 요약만 표시 후 정지, 크래시 없음 |
| 행 3개 삭제 + 2개 추가한 파일 | 신규 3 / 삭제 2 정확 분류, 항등식 유지 |
| 신규/삭제 행의 단가효과 | 전부 0 (PRD 5.4 준수) |
| 수량 0 · 금액 0 행 20건 주입 | inf 미발생, 단가 `—` 표기 |
| 변동 전 0 → 증감률 | `신규` / `삭제` / `—`, inf·NaN 화면 노출 0건 |
| 복합키 중복 주입 | 1건 보고 + 3행 제외, 합산 안 함 |
| 96개 조합(기간×축×측정값) 순회 | 예외 0건 |
| 4개 표시 모드 × 96조합 문자열 검사 | `nan`/`inf` 문자열 0건 |
| 위젯 반복 조작 후 재파싱 | 0회 (캐시 적중) |
| 변동 0 + 변동행만 필터 | 총계 1행만 남고 정상 렌더 |

### 4.4 코드 품질 / 설정

| 검증 | 결과 |
|---|---|
| `core/`에 Streamlit 유입 | 없음 ✓ |
| `app.py`에 계산 로직 유입 | `groupby`/`pivot`/`/1000` 등 없음 ✓ |
| 캐시 반환값 변조 | `build_diff`·`build_matrix`·`period_detail` 모두 입력 무변조 확인 ✓ |
| 행·열 번호 하드코딩 | 헤더는 탐색(`A열=변경`), 컬럼은 `schema.COLUMN_MAP` 상수 ✓ |
| `config.toml` 보안 설정 | `address="localhost"` · `gatherUsageStats=false` ✓ |
| `.gitignore` | `.env`·`.venv`·실데이터 차단 ✓ |
| `LoadResult` 직렬화 | 피클 가능 (캐시 안전) ✓ |
| CSV 인코딩 | `utf-8-sig` — Excel 한글 정상 ✓ |

---

## 5. 수정 결과

### 5.1 수정 완료 (7건)

| # | 대상 | 수정 | 검증 |
|---|---|---|---|
| 1 | **3.1 드릴다운** (중대) | `rows_for_axis(axes, axis_label)` — 라벨 재계산 방식으로 교체, 문자열 분해 제거 | 5행 → 1행. 축 조합 5종 전수 대조 테스트 |
| 2 | 3.2-B FR-408 | 총계를 다섯 프레임 모두 **첫 행**으로 재정렬 | 첫 행 위치 + 합계 정합 테스트 |
| 3 | 3.2-C | 한쪽에만 있는 기간을 `st.info` 배너로 안내 | 기간 차집합 검출 확인 |
| 4 | 3.2-D | `render_trend` — Plotly `go.Bar` + 값별 색상 | 브라우저에서 7월만 파랑 확인 |
| 5 | 3.2-E | `contributor_total` / `contributors_truncated` + 캡션 | 잘림/전체 두 경우 테스트 |
| 6 | 3.2-A | `_unique_total_label()` 충돌 회피, `Matrix.total_label` | 축 값을 `총계`로 오염시켜 테스트 |
| 7 | 3.2-F | `fmt_number` 음수 0 정규화, 미사용 `event` 제거 | 경계값 4종 + 표시 모드 4종 테스트 |

**회귀 확인**: 수정 후에도 총계 9,105,633.20 / 7월 −11,491.19 / 항등식 잔차 5.826e-12 로 동일하다.
테스트는 **201건 → 223건** (`tests/test_drilldown.py` 신규 + 기존 보강).

### 5.2 남은 항목 (전부 Should/Could — 미착수)

| 대상 | 내용 | 판단 |
|---|---|---|
| FR-204 | `schema.MEASURES`에 영업이익·손익·총원가 추가 (계산은 이미 됨) | 손익 분석이 필요해지면 한 줄로 추가 가능 |
| FR-104 | 파일명에서 `\d+-\d+주` 추출해 짧은 라벨로 표시 | 파일명 규칙이 확정되면 |
| 8장 포트 | `run.bat`에 8501 점유 시 8502 대체 | 단일 사용자 로컬 실행이라 충돌 가능성 낮음 |
| 8장 헤더 | 헤더 탐지 실패 시 미리보기 + 행 선택 UI | **투입 대비 효용 낮음.** 양식이 고정된 사내 파일이라 탐지 실패 자체가 드물고, 현재도 원인이 명확한 에러 메시지가 뜬다 |

---

## 6. 참고 — 구현이 PRD와 의도적으로 다른 부분

| 항목 | PRD | 구현 | 이유 |
|---|---|---|---|
| 3행 단위 읽기 | "3행 단위를 화면 표기에 사용" | `schema.UNITS`에 선언 | 원본 3행에 열 정렬이 어긋난 셀이 있어 신뢰 불가 |
| 분기/반기 | "원본 U·V 우선" | 원본 우선 + **기준년월 대조 검증** | 샘플에 4월인데 분기=1인 행 등 4건 존재. 그대로 쓰면 월 합계 ≠ 분기 합계 |
| 컬럼 식별 | 명시 없음 | 헤더 이름이 아닌 **열 문자** 기준 | 5행 헤더에 `수량`·`금액`·`총원가`·`영업이익`이 각각 두 번 등장 |
| 기간 선택 UI | "표의 행을 클릭" | `st.segmented_control` + 행 클릭은 축 드릴다운용 | 표의 행은 축 값이지 기간이 아니다 |
| `?demo=1` | 없음 | `samples/` 자동 로드 | 업로드 없이 화면 확인용 (개발·시연) |
